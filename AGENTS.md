# Wyccc's Mod Manager 项目规则

- 修改本项目时，只更新 `G:\git\wyccc-warhammer-mod-manager` 内的源码、测试和文档。
- 不得替用户运行发布打包流程，包括 `scripts\package_release.ps1`、`一键打包发布版.cmd` 或等价命令。
- 不得自动复制、覆盖或写入 `G:\Wyccc's Mod Manager` 及其中的发布文件；发布版由用户自行打包。
- 只有用户明确撤销本规则后，才可以恢复自动打包或发布目录写入操作。
